<h2>Adicione o veneno! 😈</h2>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient, collection: Ingredient.ordered %>

  <%= link_to 'Cancelar', cocktail_path(@cocktail), class: 'btn btn-outline-secondary' %>
  <%= f.submit 'Taqui o Bicho', class: 'btn btn-primary' %>
<% end %>